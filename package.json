{
    "name": "video-sharing-platform",
    "version": "1.0.0",
    "description": "",
    "scripts": {
      "frontend": "cd . && next dev",
      "backend": "cd backend && npx nodemon src/server.ts",
      "dev": "concurrently \"npm run frontend\" \"npm run backend\"",
      "install-all": "npm install && cd backend && npm install"
    },
    "dependencies": {
      "concurrently": "^8.2.2",
      "next": "latest",
      "react": "latest",
      "react-dom": "latest",
      "firebase": "latest"
    },
    "devDependencies": {
      "tailwindcss": "latest",
      "postcss": "latest",
      "autoprefixer": "latest"
    }
  }